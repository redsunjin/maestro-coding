Use the existing project harness.

Run the strongest available validation from `.agent/orchestration-contract.md`.
If it fails, diagnose the failure, make the smallest appropriate fix, rerun validation, and update `.agent/orchestration-log.md`.
Do not change harness structure unless the user explicitly asks.
