Use project-harness-runner in orchestrate-only mode.

The existing harness is failing. Preserve the harness structure, inspect the failing output, identify whether the cause is product code, tests, environment, or stale expectations, then fix the smallest valid cause and rerun the existing validation.
