Use project-harness-runner in orchestrate-only mode with TDD discipline.

Goal:
VU-001 Agent Registry and Approval Decision Protocol roadmap

For the next behavior change or bug fix, write or update a failing focused test first, confirm the failure, implement the smallest fix, confirm the focused test passes, then run the existing harness.
If TDD is not practical, record the reason in `.agent/orchestration-log.md`.
